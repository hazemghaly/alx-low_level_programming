#include <stdlib.h>
#include "main.h"
#include <stdio.h>

/**
 * main  - arguments of program
 * @argc: argument
 * @argv: argument string
 *
 * Return: Always 0 (Success)
 */

int main (int argc, char *argv[])
{
int i;
printf("%d\n",argc);
for (i = 0 ; i != *argv[i] ; i++)
{
return (0);
}
return (0);
}
