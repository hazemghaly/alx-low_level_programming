#include "main.h"
#include <math.h>
/**
 *  _sqrt_recursion -  returns the value of x sqrt
 *@n: first number
  * Return: Always 0.
 */
int _sqrt_recursion(int n);
{
if (n < 0 || (n % 2) != 0) 
{
return (-1);
{
return(_sqrt_recursion(n,0.5)*n);
}
