#include "main.h"
#include <string.h>

/**
 * *string_toupper - turn upper 
 * @s: strimg
 * @i: the number 
 * @m: lenght 
 * Return: nothing.
 */
char *string_toupper(char *)
{
int i = 0;
char s;
int m = strlen(s);
for ( ; i < m ; i++)
{
s[i] = toupper(s[i]);
}
}
