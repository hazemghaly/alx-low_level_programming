#include <string.h>
#include "main.h"
#include <stdio.h>

/**
 * main  - argument of program
 * @argc: argument
 * @argv: argument string
 *
 * Return: Always 0 (Success)
 */

int main (int argc, char *argv[])
{
printf("argv[0]=%s\n",argv[0]);
if (argc < 2)
{
return (0);
}
return(0);
}
