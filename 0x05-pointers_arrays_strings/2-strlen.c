#include "main.h"
#include <string.h>
/**
 * _strlen -  lrngth of string
 * @s: first number
 * Return: 0 (scuess)
 */
int _strlen(char *s)
{
	char str[] = "My first strlen!";
	s = str ;
_strlen(s);
return (0);
}
