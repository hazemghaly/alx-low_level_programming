#include "main.h"

/**
 * print_array - writes the string
 *@a : first number
 *@n : second number
 * Return: Always 0.
 */
void print_array(int *a, int n)
{
	int t[n];
a = t;
while (*t[n] != '\0')
{
_putchar(*t[n]);
_putchar(',');
n++;
}
_putchar('\n');
}
