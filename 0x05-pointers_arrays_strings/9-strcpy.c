#include "main.h"

/**
 * *_strcpy - writes the string
 *@dest : first number
 *@src : second number
 * Return: Always 0.
 */
char *_strcpy(char *dest, char *src)
{
char size_t; 
int i  , n;
size_t;
for (i = 0; i < n && src[i] != '\0'; i++)
dest[i] = src[i];
for ( ; i < n; i++)
dest[i] = '\0';
return dest;
}
