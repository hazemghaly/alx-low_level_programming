#include <string.h>
#include "main.h"
#include <stdio.h>

/** *_strstr - string string fun
 * @haystack: first char
 * @needle: second char
 * @len: int
 * return :n sceuss
 */
char *_strstr(char *haystack, char *needle)
{
return (strstr(*haystack, *needle));
}
