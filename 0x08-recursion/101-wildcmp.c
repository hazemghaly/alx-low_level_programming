#include "main.h"
#include <string.h>
/**
 * wildcmp - writes the string s and a trailing newline
 *@s1: first number
 *@s2: 2nd number
 *@i: 3rd number
 * Return: Always 0.
 */
int wildcmp(char *s1, char *s2);
{
int 1 = 0;
if (s1[i] != '\0' && s2[i] != '\0')
}
if (s1[i] == s2[i])
{
return (1);
i++;
}
}
return (0);
}
