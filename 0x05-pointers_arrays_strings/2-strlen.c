#include "main.h"
#include <string.h>
/**
 * int _strlen -  swaps the values of two integers
 * @s: first number
 * 
 * Return: 0 (scuess)
 */
int _strlen(char *s)
{
int size = strlen(char *s);
return(0);
}
