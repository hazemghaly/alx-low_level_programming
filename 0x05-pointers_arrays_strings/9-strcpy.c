#include "main.h"
#include <string.h>
/**
 * *_strcpy - writes the string
 *@dest : first number
 *@src : second number
 * Return: Always 0.
 */
char *_strcpy(char *dest, char *src)
{
while (*(src + l) != '\0')
{
l++;
}
for ( ; x < l ; x++)
{
dest[x] = src[x];
}
dest[i] = '\0';
return dest;
}
