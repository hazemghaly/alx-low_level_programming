#include "main.h"
#include <string.h>
/**
 * int _strlen -  lrngth of string
 * _strlen(s) - length of strings
 * @s: first number
 * 
 * Return: 0 (scuess)
 */
int _strlen(char *s)
{
_strlen (s);
return (0);
}
