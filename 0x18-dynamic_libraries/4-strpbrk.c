#include <string.h>
#include "main.h"
#include <stdio.h>

/** *_strpbrk - fill memory
 * @s: first char
 * @accept: second char
 * @len: int
 * return :n sceuss
 */
char *_strpbrk(char *s, char *accept)
{
return (strpbrk(s, accept));
}
