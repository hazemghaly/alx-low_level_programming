#include "main.h"
#include <string.h>
/**
 * int _strlen -  swaps the values of two integers
 * @s: first number
 * 
 * Return: 0 (scuess)
 */
int _strlen(char *s)
{
int len;
	len = _strlen(s);
return(0);
}
