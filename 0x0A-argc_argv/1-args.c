#include <string.h>
#include "main.h"
#include <stdio.h>

/**
 * main  - argument of program
 * @argc: argument
 * @argv: argument string
 *
 * Return: Always 0 (Success)
 */

int main (int argc, char *argv[])
{
(void)*argv[0];
printf("%d\n",argc);
return (0);
}
