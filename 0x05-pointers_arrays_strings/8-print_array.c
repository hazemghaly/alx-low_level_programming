#include "main.h"

/**
 * print_array - writes the string
 *@a : first number
 *@n : second number
 * Return: Always 0.
 */
void print_array(int *a, int n )
{
int i;
for (i = 0; i < (n - 1); i++)
{
_putchar('*a[n]');
_putchar(',');
_putchar(' ');
}
_putchar('\n');
}
